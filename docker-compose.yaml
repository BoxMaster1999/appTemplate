services:
  simple-app:
    build:
      context: ./app
      dockerfile: Dockerfile
      target: dev
    volumes:
      - .:/app
      # model files
      - ./models:/models
    ports:
      - '8081:8080'
